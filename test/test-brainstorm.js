const assert = require('assert');
const _ = require('lodash');

/**
 * WIP: still trying to wrap my mind around this
 *  - indexing
 *  - querying
 *  - jobs
 */
describe.skip('my suite', function() {
    beforeEach(function() {
    })

    it('works', function() {
        assert.fail('should fail');
    })
})